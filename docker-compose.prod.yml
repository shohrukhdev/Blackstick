services:
  web:
    container_name: ubuntu_web_1
    env_file:
      - .env
    image: "${BE_IMAGE}"
    ports:
      - "8050:8050"
    command: gunicorn Blackstick.wsgi:application --bind 0.0.0.0:8050
    volumes:
      - static_volume:/home/app/web/staticfiles
      - media_volume:/home/app/web/mediafiles
volumes:
  static_volume:
  media_volume:
